import { motion } from "framer-motion";
import { Button } from "@/components/ui/button";
import { Sprout, Brain, BarChart3, ChevronDown } from "lucide-react";
import farmingHero from "@/assets/farming-hero.jpg";

// ✅ Reusable Feature Card Component
const FeatureCard = ({ Icon, title, description, color }) => (
  <motion.div
    initial={{ opacity: 0, y: 40 }}
    whileInView={{ opacity: 1, y: 0 }}
    viewport={{ once: true }}
    transition={{ duration: 0.6 }}
    whileHover={{ scale: 1.05, boxShadow: "0px 0px 20px rgba(0,0,0,0.2)" }}
    className={`p-6 rounded-xl bg-card shadow-elevation border hover:shadow-glow transition-smooth`}
  >
    <Icon className={`h-10 w-10 ${color} mb-4 mx-auto`} />
    <h3 className="text-xl font-semibold mb-2">{title}</h3>
    <p className="text-muted-foreground">{description}</p>
  </motion.div>
);

const FarmingAgentHero = () => {
  return (
    <section
      className="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-earth"
      aria-label="AI Farming Agent Hero Section"
    >
      {/* Background Image with Parallax */}
      <div
        role="img"
        aria-label="Smart AI-powered farming background"
        className="absolute inset-0 bg-cover bg-center bg-no-repeat opacity-20"
        style={{
          backgroundImage: `url(${farmingHero})`,
          backgroundAttachment: "fixed",
        }}
      />

      {/* Content */}
      <div className="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        {/* Icon */}
        <motion.div
          initial={{ opacity: 0, scale: 0.8 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ duration: 0.8 }}
          className="mb-8 flex justify-center"
        >
          <div className="p-4 rounded-full bg-gradient-primary shadow-glow">
            <Brain className="h-12 w-12 text-primary-foreground" />
          </div>
        </motion.div>

        {/* Title */}
        <motion.h1
          initial={{ opacity: 0, y: -30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.9 }}
          className="text-5xl md:text-7xl font-bold mb-6 bg-gradient-to-r from-primary to-primary-glow bg-clip-text text-transparent"
        >
          AI Farming Agent
        </motion.h1>

        {/* Subtitle */}
        <motion.p
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 1 }}
          className="text-xl md:text-2xl text-muted-foreground mb-8 max-w-3xl mx-auto leading-relaxed"
        >
          Revolutionize your farm with intelligent AI guidance. Get personalized
          crop recommendations, weather insights, and yield predictions to
          maximize your harvest.
        </motion.p>

        {/* CTA Button */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 1.2, duration: 0.8 }}
          className="flex justify-center mb-12"
        >
          <Button variant="hero" size="lg" className="text-lg px-8 py-4">
            Start Farming Analysis
          </Button>
        </motion.div>

        {/* Feature Cards */}
        <motion.div
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
          transition={{ staggerChildren: 0.2 }}
          className="grid md:grid-cols-3 gap-6 mt-16"
          aria-label="Key Features"
        >
          <FeatureCard
            Icon={Sprout}
            title="Crop Intelligence"
            description="AI-powered crop selection and optimization based on soil, climate, and market conditions."
            color="text-primary"
          />
          <FeatureCard
            Icon={BarChart3}
            title="Yield Prediction"
            description="Advanced analytics to forecast harvest yields and optimize farming strategies."
            color="text-accent"
          />
          <FeatureCard
            Icon={Brain}
            title="Smart Decisions"
            description="Get intelligent recommendations for planting, irrigation, and pest management."
            color="text-primary-glow"
          />
        </motion.div>

        {/* Scroll Indicator */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 1.5 }}
          className="absolute bottom-6 left-1/2 transform -translate-x-1/2"
        >
          <ChevronDown className="h-8 w-8 animate-bounce text-muted-foreground" />
        </motion.div>
      </div>
    </section>
  );
};

export default FarmingAgentHero;
